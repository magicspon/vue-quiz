
# ignoring the config folder
# contains some custom syntax (lost) that doesn't play nice

_assets/scss/_config

# ignore the symbols template file
gulp/libs 

# thirdparty code might live here
# so lets not lint
_assets/scss/_system